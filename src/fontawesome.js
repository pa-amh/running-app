import { library } from '@fortawesome/fontawesome-svg-core';
import {
    faCalendarDay,
    faRunning,
    faPen,
    faStopwatch,
    faTrash
} from '@fortawesome/free-solid-svg-icons';

// Import the WHOLE library
// import { fab } from '@fortawesome/free-brands-svg-icons';
// library.add(fab);

// Import select icons to the library for speed
library.add(
    faCalendarDay,
    faRunning,
    faPen,
    faStopwatch,
    faTrash
);
